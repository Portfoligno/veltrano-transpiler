fun abort(message: String): Nothing {
    panic("{}", message)
}

fun conditionalAbort(shouldAbort: Bool): Int {
    if (shouldAbort) {
        abort("Something went wrong!".toString())
    } else {
        return 42
    }
}

fun main() {
    val result: Int = conditionalAbort(false)
    println("{}", result)
}