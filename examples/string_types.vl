fun main() {
    val slice: Ref<Str> = "Hello"
    val owned: String = "World".toString()
    val borrowed: Ref<String> = owned.ref()
    val boxed: Box<Str> = "Boxed".into()
    
    println("{}", slice)
    println("{}", owned)
    println("{}", borrowed)
    println("{}", boxed)
}