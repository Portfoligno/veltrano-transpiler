fun calculateSum(firstNumber: Int, secondNumber: Int): Int {
    val totalResult: Int = firstNumber + secondNumber
    return totalResult
}

fun main() {
    val myFirstValue: Int = 10
    val mySecondValue: Int = 20
    val finalResult: Int = calculateSum(myFirstValue, mySecondValue)
    println("{}", finalResult)
}
