fun main() {
    val owned: String = "Hello World".toString()
    val borrowed: Ref<String> = owned.ref()
    val stringSlice: Ref<Str> = "literal"
    
    println("{}", owned)
    println("{}", borrowed)
    println("{}", stringSlice)
}