fun main() {
    val owned: Own<String> = "Hello World".toString()
    val borrowed: String = owned.ref()
    val stringSlice: Str = "literal"
    
    println("{}", owned)
    println("{}", borrowed)
    println("{}", stringSlice)
}
